services:
  - type: web
    name: blockchain-dashboard-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    runtime: python-3.11.9
